{
  "name": "@repo/auth-service",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "nodemon --exec \"npx ts-node --transpile-only src/index.ts\"",
    "dev": "nodemon --exec \"npx ts-node --transpile-only src/index.ts\"",
    "build": "tsc",
    "auth:generate": "npx @better-auth/cli generate --config ./src/lib/auth.ts --output ../../packages/db/neon/src/db/auth-schema.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@repo/service": "workspace:*",
    "@repo/db-neon": "workspace:*",
    "better-auth": "^1.3.24",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/express": "^5.0.3",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
